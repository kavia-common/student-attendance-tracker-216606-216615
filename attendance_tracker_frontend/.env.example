# Flutter frontend env for backend integration
# Point this to your Node API base URL (no trailing slash)
API_BASE_URL=http://localhost:8080
# When true, the app will call the backend API; when false, use local SQLite only (tests)
USE_REMOTE=true
# Optional: enable realtime attendance updates via SSE
FEATURE_FLAG_REALTIME=false
