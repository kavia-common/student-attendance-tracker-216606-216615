Root detection helper

If your CI or analysis tool cannot determine the Flutter project root automatically, set the working directory explicitly to:
student-attendance-tracker-216606-216615/attendance_tracker_frontend

Then run:
- flutter pub get
- flutter analyze
- flutter test
