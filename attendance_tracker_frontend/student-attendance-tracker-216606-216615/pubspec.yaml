name: student_attendance_workspace
publish_to: 'none'
description: Workspace meta for Student Attendance Tracker. Not a Flutter app. See attendance_tracker_frontend for the Flutter project.
environment:
  sdk: '>=3.0.0 <4.0.0'

# This is a workspace-level helper so CI can detect a Dart project root.
# Always change directory to attendance_tracker_frontend before running Flutter commands.
