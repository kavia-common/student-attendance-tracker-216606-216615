Student Attendance Tracker - Workspace Root

Important:
- The Flutter app root is located at:
  student-attendance-tracker-216606-216615/attendance_tracker_frontend

CI/Tooling instructions:
- Before running any Flutter commands (flutter pub get, flutter analyze, flutter test),
  set the working directory to the app root:
    cd student-attendance-tracker-216606-216615/attendance_tracker_frontend
- Alternatively, run the helper to verify root:
    dart run bin/ensure_flutter_root.dart

Notes:
- The presence of pubspec.yaml inside the app directory is the signal for the Flutter project root.
- If your CI cannot auto-detect the project root, ensure the directory is set explicitly.
